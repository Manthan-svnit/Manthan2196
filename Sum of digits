#include<stdio.h>
int main()
{
    int num,sum=0,rem;
    printf("Enter your number:\n");
    scanf("%d",&num);
    while(num!=0)
    {
        rem = num%10;
        sum = sum + rem;
        num = num/10;
    }
    printf("Sum of digits is =%d",sum);
}
