#include<stdio.h>
int main()
{
    int num,rem,sum=0;
    printf("Enter your number:\n");
    scanf("%d",&num);
    while(num!=0)
    {
        rem = num % 10;
        sum = sum * 10 + rem;
        num = num / 10;
    }
    printf("Rverse number is %d",sum);
}
